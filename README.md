# chatgpt-usage-demo

This repository provides a minimal example of using Python and Flask to serve a basic web page.

## Running the app

Install the dependencies and start the server:

```bash
pip install -r requirements.txt
python app.py
```

Then open `http://localhost:5000/` in your browser.
